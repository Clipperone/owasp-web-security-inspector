/**
 * Cookie utilities — parse and serialize cookie data.
 *
 * Phase 2 will implement:
 *   - parseCookieString(raw: string): Partial<CookieData>
 *   - serializeCookieAttributes(cookie: CookieData): string
 *   - isCookieExpired(cookie: CookieData): boolean
 */

export {};
