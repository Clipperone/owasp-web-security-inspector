/**
 * Header rule utilities — build and validate declarativeNetRequest payloads.
 *
 * Phase 2 will implement:
 *   - toDNRRule(rule: HeaderRule): chrome.declarativeNetRequest.Rule
 *   - validateHeaderRule(rule: Partial<HeaderRule>): string[]  (returns validation errors)
 *   - generateRuleId(existing: HeaderRule[]): number
 */

export {};
